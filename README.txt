Double Click the index.html file and the program should run just fine.
The initial load is a bit slow since all links have to be loaded (Ensure you are connected to the internet because none of the data for icons, pictures, or videos are stored locally).
Refresh the page only when you want to restart site and clear all saved data.
